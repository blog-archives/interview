---
title: 面经合集
order: 0
---



## 开场白

面试官你好，我是苏桐渤，本科毕业于杭州师范大学软件工程专业。在校期间，我曾参与ACM实验室并获得了ICPC铜奖和CCPC银奖，这段经历极大提升了我的编程和问题解决能力。

毕业后，我一直在信友队做后端 web 开发，公司的核心业务主要是面向中小学生信奥竞赛的教学平台，包括题库系统、竞赛平台、线上授课等一些内容。作为协作者，我参与了绝大部分项目的开发，也能独立完成开发到交付的完整过程。不论是团队协作还是独立交付都具备较好的能力。此外，我也能积极参与团队的技术文档完善和流程优化，为提高团队的效率而努力。我认为自己需要跳出舒适圈，挑战更复杂的业务场景，拓宽技术视野，因此很荣幸能有机会面试贵公司。



## 离职原因

原公司业务和技术栈相对固定，日常以业务迭代和维护为主。希望能接触更多负责项目和场景，拓展技术视野。



## 项目概述



### 竞赛系统

- 业务服务与 UOJ 评测机之间任务流转异常

> **原背景一句话：** 原有方案由评测Pod空闲轮询提交表争抢`waiting`任务，直接转为`running`执行评测，缺少pending中间态，无优先级调度，调度与业务存储强耦合，可观测性不足。
>
> **优化目的一句话：** 本次引入消息队列完善waiting‑pending‑running‑finished/system_error状态机，实现优先级调度，解耦调度与业务存储，降低分发延迟，适配集群自动扩缩容并强化幂等与异常兜底。
>
> **优化实现：** 保留提交表，新增优先级、重试次数、下次检查时间字段；用户提交仅落库为`waiting`，带分布式锁的业务worker扫描待处理任务按优先级投递至高优、普通两套MQ队列，投递成功流转为`pending`，失败自动退避重试；每个评测Pod同时订阅双队列，优先消费高优任务，消费使用手动ACK，收到消息先查DB做幂等校验，仅`pending`可转为`running`启动评测，其他状态直接ACK丢弃，评测完成回调更新为`finished`后再提交MQ确认；独立巡检协程仅扫描超时的`pending`、`running`做异常校验，不参与任务分发，`pending`告警、`running`调用评测机接口识别卡死任务并标记异常交由人工重判；K8s HPA依据高优队列堆积量触发扩缩容，避免低优任务造成无效扩容。

- 比赛开赛瞬间高并发提交流量治理

> **背景一句话：** 比赛开赛瞬间大量用户并发访问题目、榜单、比赛信息等各类接口，突发读流量容易直接冲击MySQL数据库，引发慢查询、连接占满，影响整套比赛系统可用性。
>
> **目标一句话：** 通过热点预加载、全接口缓存策略，在不做接口降级关闭的前提下扛住开赛瞬时流量，保证所有接口均可正常返回，同时保护数据库不被读流量打穿。
>
> **实现：** 开赛之前把比赛基础信息、题目、样例数据等固定热点内容提前预热存入Redis；所有读接口统一增加缓存层，优先读取Redis数据，缓存未命中才回源MySQL，同时增加缓存击穿保护，避免热点key失效瞬间大量请求穿透数据库；榜单不实时查询数据库做聚合计算，改为后台异步任务周期性计算完整结果写入Redis，接口直接读取缓存快照返回，接受秒级数据延迟；数据库开启读写分离，所有读请求路由至从库，主库仅承担代码提交等写操作；网关层配置单用户、全局限流，拦截恶意刷请求，保护服务不被异常流量打垮；前端增加请求防抖与本地短时缓存，减少重复无效请求；代码提交链路继续沿用原有MQ异步评测调度，读写链路相互隔离，读流量不会挤占提交评测相关资源，系统压力再高也不关闭任何业务接口，全部接口通过缓存返回结果。

- 赛后代码查重



### 题库系统

1. **数据膨胀与分页性能问题**：提交记录持续海量增长，**教师后台查询统计、分页查看提交数据**受影响明显，深分页查询性能差。

- **题目AC统计数据问题**：题目的提交数、通过数、通过率统计，实时计算开销大，频繁更新统计字段会引发数据库行锁冲突，缓存方案还会带来统计不准的问题。

> 我们复用现有评测回调消息链路，在消费评测结果更新业务提交数据时同步在Redis完成题目通过率、错误提交分布的增量计数，避免高并发场景下直接写数据库引发的行锁冲突；再通过短周期定时任务将Redis中的增量数据合并落库，保障统计的分钟级时效性，同时执行每日夜间全量重算任务，基于原始提交记录校正统计结果，以此兼顾统计性能、时效性和数据准确性。

- **恶意/高频重复提交**：用户短时间大量重复提交代码，占用评测队列资源，挤压正常用户的评测排队时长。

> 我们采用Redis实现多维令牌桶限流，分别维护单用户一分钟短周期、一小时长周期两个令牌桶，同时叠加同一用户针对单道题目的令牌桶限制；用户发起代码提交请求时，需要同时从全部对应令牌桶获取令牌，任意一个桶拿不到令牌就直接拒绝提交请求，不再执行入库和投递评测MQ的流程，以此既拦截短时间疯狂刷提交的行为，也防范一小时维度持续批量提交消耗评测资源，配额随令牌桶自动恢复，无需人工干预。



## 面试题



## 一、Golang 语言 & 业务落地场景

### 1. goroutine、GMP、并发场景

**基础问题**：讲下GMP模型；什么场景用goroutine，什么场景要控制goroutine数量 深挖追问链：

1. 大量goroutine暴涨会带来什么问题？内存占用？调度开销？
2. 写业务如果不控制goroutine，比如for循环直接go func()，会有什么坑？闭包捕获变量问题怎么复现、怎么解决？
3. G什么时候会被抢占？syscall的时候GMP发生了什么？M会销毁吗？P会去哪里？
4. channel底层？无缓冲、有缓冲、nil channel、关闭的channel读写行为？业务上什么场景用无缓冲，什么用有缓冲？
5. 业务中遇到过goroutine泄露吗？什么场景会泄露？怎么排查goroutine泄露？pprof看哪块？
6. 为什么不直接用无限制goroutine做任务队列？业务里如何做goroutine池？自己实现还是用第三方库？优缺点？

### 2. Context上下文（业务高频）

**基础问题**：context作用，业务哪些场景必须用context 深挖追问链：

1. context的底层结构，cancelCtx怎么实现取消传播？
2. 父context取消，子context会发生什么；子取消会不会影响父？
3. 业务坑：把context塞到结构体全局复用会有什么问题？
4. WithValue适合存什么？不适合存什么？为什么不拿它传业务参数？
5. 线上遇到过ctx超时引发的诡异报错吗？数据库、rpc调用ctx超时的完整链路表现？
6. 如果一个RPC调用，ctx设置3s超时，但是内部有2个串行DB查询，每个2s，会发生什么？

### 3. sync包并发原语业务选型

**基础问题**：sync.Mutex、RWMutex、sync.Map，业务分别什么场景选？ 深挖追问链：

1. Mutex的状态，饥饿模式是什么，什么时候触发？
2. RWMutex写锁会不会被读锁饿死？Go旧版本和新版本差异？业务风险？
3. sync.Map底层原理，适合什么场景；**不适合什么场景？大量写场景为什么性能差？**
4. sync.WaitGroup业务踩坑点？Add和goroutine执行顺序搞错会发生什么？
5. Once底层，panic场景会怎么样？
6. 业务并发数据竞争：怎么发现data race？go run -race原理？线上怎么定位数据竞争？

### 4. 内存、GC、线上业务问题

**基础问题**：Go GC三色标记，写屏障，GC流程 深挖追问链：

1. 为什么需要混合写屏障？STW两个阶段分别干什么？STW时间受什么影响？
2. 业务什么情况会造成频繁GC？大对象、大量小对象、内存泄漏？
3. Go内存泄漏有哪些典型业务场景？goroutine泄漏、 defer循环、slice底层数组不释放？
4. pprof排查内存泄漏完整流程：heap、allocs，怎么区分是真泄漏还是正常业务缓存？
5. slice底层，切片截取引发内存泄漏案例；append扩容机制，业务怎么避免？
6. defer的坑：循环里面defer会有什么后果；defer捕获参数是值拷贝还是引用？

### 5. error处理业务实践

**基础问题**：Go的error设计，业务怎么优雅处理错误 深挖追问链：

1. errors.Is / errors.As 作用，和直接==对比；为什么不要返回裸字符串错误？
2. 业务层错误包装，fmt.Errorf(%w)什么时候用？
3. panic业务中什么时候允许panic？接口服务能不能直接panic不捕获？
4. 业务RPC/HTTP服务，错误码分层设计思路？

------

## 二、MySQL 业务 + 索引 + 锁（后端必问，重落地）

### 1. 索引 & explain 业务优化

**基础问题**：B+树索引原理，聚簇索引、二级索引；什么时候会索引失效 深挖追问链：

1. 回表是什么？覆盖索引业务场景，什么SQL可以避免回表？
2. 最左匹配原则；联合索引建立顺序，业务怎么选择字段顺序？
3. 哪些SQL会导致索引失效：隐式转换、like %xxx、or条件，举业务案例
4. explain关键字段：type、key、rows、Extra，出现Using filesort、Using temporary代表什么业务问题？怎么优化？
5. 大表加索引业务风险？online ddl原理，业务上什么时候改表结构？

> 1. **Online DDL之前（COPY模式）** 一上来拿到锁，**整个DDL全程持有锁，从头到尾阻塞全部读写**。
> 2. **Online DDL之后**
>
> - 开头拿一次排他元锁，做完准备立刻释放；
> - **中间绝大部分时间不持有排他锁，可以正常读写，业务变更写入增量日志**；
> - 收尾再拿一次排他元锁，把增量日志回放完，完成变更，释放锁。
>
> ⚠️注意：开头、结尾**拿锁的那一瞬间如果拿不到（被长事务占住读锁）**，DDL就卡在队列里，此时新来的读写全部阻塞。 中间运行阶段是安全的，风险集中在两次短暂拿锁的环节。

1. 千万级大表，分页limit offset很大，性能差，怎么优化？（业务方案，不止子查询）

### 2. 事务、隔离级别

**基础问题**：4种隔离级别，InnoDB默认级别；MVCC原理 深挖追问链：

1. RR级别下，幻读问题InnoDB怎么解决？快照读 vs 当前读区别
2. MVCC的undo log、read view，undo log什么时候清理？
3. 业务中，RC和RR怎么选，各自业务收益和代价？
4. 事务什么时候会回滚？panic、程序crash、连接断开分别表现？

> 普通 SQL 报错只撤销失败语句，事务继续存活，需要手动 rollback；只有死锁、连接断开、宕机会自动整体回滚事务。
>
> 只有数据库连接被 MySQL 服务端判定关闭时，未提交事务才自动全部回滚。如果进程还活着、连接还在，哪怕程序 panic，MySQL 完全不知道程序出错，事务不会自己回滚，业务代码必须手动 rollback。

### 3. InnoDB锁（面试深挖重灾区，结合业务场景）

**基础问题**：行锁、表锁、意向锁；共享锁S，排他锁X 深挖追问链：

1. **记录锁、间隙锁、临键锁，RR下什么时候产生间隙锁？举业务SQL例子**
2. 业务死锁怎么产生？举两条SQL造成死锁案例；线上如何排查死锁？

> `show engine innodb status` 看产生死锁的两个事务 id 和具体 SQL 然后渠道项目中找到对应的业务代码
>
> 1. 统一事务执行顺序
> 2. 减少在事务中执行其他的业务逻辑操作
> 3. 降低锁粒度，检查是否正确走索引
> 4. 设置锁超时时间兜底

1. update没命中索引，会升级表锁？为什么？业务后果？
2. select for update业务使用场景；for update skip locked解决什么业务（库存抢扣），有什么缺陷？
3. 大事务业务危害：锁持有时间长、undo log、binlog刷盘、主从延迟；哪些业务会写出大事务？怎么拆分大事务？

> 大事务拉长锁持有时间，放大锁冲突；堆积 undo 日志；阻塞 DDL；引发主从延迟；回滚开销大

### 4. 主从、binlog、业务踩坑

**基础问题**：binlog三种格式statement/mixed/row，主从复制流程 深挖追问链：

1. binlog row格式优势劣势；业务什么场景必须row格式？
2. 主从延迟产生的原因；业务读写分离场景，如何解决主从延迟导致的数据不一致？

> 一是传输侧，网络问题导致 binlog 日志传输慢；二是回放侧，也是线上最常见：硬件性能差距、从库读压力大、表缺少主键索引拖慢回放；主库大事务、批量操作、大 DDL 产生大量 binlog，从库消费处理不过来，产生主从延迟。

1. 半同步复制原理；数据丢失风险点？

> 半同步复制只能降低丢数据概率，无法完全杜绝：一是等待从库 ack 超时自动降级异步；二是唯一应答的从库故障，没有其他副本留存日志；三是主库`sync_binlog`非 1，事务 binlog 停留在 OS 缓存未刷盘即宕机；四是从库 ack 应答包网络丢包引发主库错误降级。

1. 业务分库分表：什么时候必须分？水平分表路由策略；跨表join怎么办；分页排序难点？

> 当SQL、索引优化、读写分离、硬件升级、冷热归档这些手段全部做完之后，出现下面两类硬性瓶颈，就必须做分库分表：
>
> 1. **存储维度**：单表行数持续增长到千万级别，索引、查询、DDL、备份恢复压力恶化；或者单库整体数据量过大。
> 2. **写入并发维度**：单机MySQL的写TPS打满硬件上限，读写分离只能分担读请求，无法解决写瓶颈。
>
> Hash 取模打散数据均匀，但扩容迁移量大；Range 范围分片扩容简单，但容易出现热点分片；时间分片是 Range 特例，适合流水日志类业务。分片键尽量是业务高频查询条件，减少跨分片查询。



## 三、Redis（业务中间件最高频，重点考察踩坑）

### 1. 基础数据结构与业务选型

**基础问题**：常用数据结构底层；业务场景选型 深挖追问链：

1. zset底层跳表；跳表对比平衡树优势？zrangebyscore业务场景；zset百万数据性能？
2. Hash结构，什么时候用Hash，什么时候直接String序列化json？优缺点对比
3. List底层；list为什么不适合做消息队列？业务缺陷？
4. BitMap、HyperLogLog、Geo分别适合什么业务，有什么误差限制？
5. Redis的String最大value？业务存大value会有什么问题？

### 2. 过期、淘汰、内存

**基础问题**：key过期策略；8种内存淘汰策略 深挖追问链：

1. 过期key是立即删除吗？惰性删除+定期删除机制；大量key同时过期会有什么线上问题？缓存雪崩导火索之一
2. 业务怎么避免大量key同一时间过期？
3. maxmemory设置为0代表什么；业务不配置maxmemory后果？

### 3. 缓存经典业务问题（必问）

**基础问题**：缓存穿透、缓存击穿、缓存雪崩，分别怎么解决 深挖追问链：

1. 缓存和数据库双写一致性；先更库后删缓存 vs 先删缓存后更库，分别会出现什么不一致场景？
2. 延迟双删策略，为什么需要延迟？延迟时间设置依据？有没有更好方案？
3. 更新数据库+删除缓存，中途服务crash怎么办？数据不一致如何兜底？
4. 分布式缓存，序列化选择；缓存value过大带来网络IO问题；
5. 缓存预热业务怎么做？冷启动问题？

### 4. Redis分布式锁（超级高频，连环深挖）

**基础问题**：Redis实现分布式锁怎么做，SET key value NX EX 深挖追问链：

1. 为什么不能用setnx+expire两条命令？原子性问题；
2. 锁过期但是业务还没执行完，锁提前释放怎么办？看门狗（续期）原理；Go客户端怎么实现看门狗，哪些库支持？
3. A客户端锁还没执行完过期，B拿到锁，A执行完del，把B锁释放，这个bug如何避免？value存唯一标识的必要性
4. Redis单机锁的问题；主从切换锁丢失问题；Redlock原理，生产环境实际落地吗？有什么争议？
5. 业务什么场景不适合Redis分布式锁？有没有替代方案（DB乐观锁）

### 5. Redis集群 & 线上故障

**基础问题**：Redis Cluster哈希槽，集群原理 深挖追问链：

1. 槽位为什么16384，不是更大数字？
2. 集群脑裂是什么现象？产生原因？配置min‑replicas‑to‑write作用？
3. bigkey问题，怎么发现bigkey；bigkey删除为什么阻塞Redis？业务如何避免bigkey？
4. RDB、AOF持久化，各自优缺点；混合持久化；宕机恢复数据丢失可能性？
5. Redis单线程模型，CPU100%可能的原因？怎么排查？
6. pipeline作用；pipeline和事务multi区别；Lua脚本原子性；Lua脚本业务风险？

------

## 四、消息队列（Kafka / RocketMQ / RabbitMQ）

> 面试一般会问你**项目用哪一个，就深挖哪一个**，先问选型理由

### 基础问题：项目为什么选这个MQ，而不是另外两个

深挖追问链：

1. **消息丢失**：哪些环节会丢消息？生产者、Broker、消费者；分别怎么保障不丢消息？acks参数，刷盘策略，确认机制。
2. **消息重复消费**：为什么一定会有重复？at‑least‑once；业务怎么实现幂等？给几种业务幂等方案，各自优缺点。
3. **消息积压**：什么场景会积压？消息大量积压之后有什么危害？线上怎么处理积压？
4. 顺序消息怎么保证？全局顺序和局部顺序；牺牲什么性能？业务什么场景要顺序？
5. 死信队列作用；什么消息进死信；死信队列业务怎么处理？
6. Kafka：partition作用；副本机制；ISR；消费者组，rebalance触发条件，rebalance带来什么业务问题？
7. RocketMQ：commitLog，ConsumeQueue；刷盘；事务消息原理；半消息；事务消息会有什么短板？
8. RabbitMQ：交换机类型；confirm、return机制；
9. MQ事务消息能完全保证和DB原子吗？最终一致性完整流程；有什么边界case？

### 业务场景追问

- 如果业务要求DB操作和发MQ必须同时成功同时失败，你怎么实现？本地事务表方案讲一下优缺点。

------

## 五、通用业务系统场景问题（无标准答案，看思路）

面试官抛出业务，看你分析风险、中间件选型、故障兜底：

### 场景1：短链接服务

基础问题：设计一个短链接服务 深挖：

1. ID生成方案，为什么不用自增ID；雪花算法优缺点，时间回拨问题怎么处理
2. 存储选Redis还是MySQL？冷热数据分离怎么做
3. 高并发访问，缓存如何设计；缓存击穿风险
4. 过期删除怎么处理；海量过期数据
5. 重定向301 vs 302业务区别；

### 场景2：商品库存扣减（并发扣库存）

基础：高并发扣减库存，不能超卖 深挖：

1. DB方案：乐观锁update set stock=stock‑1 where id=xx and stock>0，优缺点；高并发下大量重试怎么办？
2. Redis预扣库存，异步回DB，会有哪些不一致风险；
3. 分布式锁能不能直接用来扣库存？性能瓶颈在哪？
4. 订单超时未支付，库存回补，怎么保证可靠；回补失败如何兜底？

### 场景3：接口限流 & 熔断降级

基础：业务接口限流怎么做 深挖：

1. 令牌桶、漏桶算法，业务选型区别；Go怎么实现？
2. 单机限流 vs 分布式限流（Redis）；Redis分布式限流存在什么并发误差？
3. 熔断降级适用场景；和限流的区别；
4. 限流放在网关层，还是业务服务层，各有什么利弊？

### 场景4：RPC服务业务问题（gRPC）

1. context透传，超时传递；链式调用超时问题；
2. 重试机制：什么错误可以重试，什么绝对不能重试？重试风暴怎么避免？
3. 服务发现、负载均衡；
4. 线上大量rpc超时，排查思路完整链路。

------

## 六、面试官通用深挖套路（面试技巧，候选人一回答就顺着问）

不管候选人回答任何中间件/业务题，我一般固定追问这几类：

1. **线上踩过什么坑？当时现象是什么，排查路径，最后怎么修复？**（非常看重，背原理没用要看实战）
2. **这个方案有什么缺点？什么场景不适合？**
3. **如果流量放大10倍，这套方案会遇到什么瓶颈，如何优化？**
4. **如果服务宕机、中间件宕机，会发生什么数据问题，怎么兜底？**
5. **有没有其他替代方案，对比优劣？**

如果你需要，我可以：

1. 把这份清单压缩成一页极简面试提纲；
2. 或者我扮演面试官，随机抽上面的题目，模拟对你面试。
