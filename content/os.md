---
title: OS 常见面试题
---

## 进程间的通信方式

- 管道，包括匿名管道和命名管道，适合父子进程或有亲缘关系的进程间简单数据传递
- 消息队列，通过消息的发送和接收实现，支持不同进程间异步通信
- 共享内存，多个进程直接访问同一块内存区域，是最快的IPC方式，但需要同步机制如信号量配合
- 信号量，用于进程间的同步和互斥，控制对共享资源的访问
- Socket，适用于不同主机或同一主机的进程间通信，支持网络通信
- 信号，通过发送信号通知进程处理特定事件，如SIGKILL终止进程。
